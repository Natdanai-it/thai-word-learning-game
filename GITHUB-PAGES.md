# เผยแพร่เกมบน GitHub Pages

ไฟล์ ZIP ชุดนี้เป็นเว็บสำเร็จรูปแบบ Static อัปโหลดขึ้น repository แล้วเปิด GitHub Pages จากสาขา `main` ได้ทันที ไม่ต้องติดตั้งโปรแกรมเพิ่ม

## ครั้งแรก

1. สร้าง repository ใหม่ใน GitHub เช่น `thai-word-learning-game`
2. แตกไฟล์ ZIP ก่อน แล้วอัปโหลด `index.html`, `score-config.js`, `.nojekyll`, โฟลเดอร์ `images` และ `videos` ไว้ที่ระดับบนสุดของสาขา `main` (อย่าอัปโหลดไฟล์ ZIP อย่างเดียว)
3. เปิด `Settings` → `Pages`
4. ที่ `Build and deployment` เลือก `Source: Deploy from a branch`
5. เลือก `Branch: main` และโฟลเดอร์ `/(root)` แล้วกด `Save`
6. รอประมาณ 1–3 นาที แล้วรีเฟรชหน้า Pages จนแสดงลิงก์เว็บไซต์

ลิงก์จะเป็นรูปแบบ:

`https://ชื่อผู้ใช้.github.io/thai-word-learning-game/`

วิดีโอ โลโก้ และภาพหน้าปกอยู่ใน repository จึงไม่ต้องพึ่งสิทธิ์การแชร์จาก Google Drive

## ฟังก์ชันในรุ่นล่าสุด

- 4 คลิป และ 5 โหมดเกม รวม 20 ภารกิจ พร้อมห้องทบทวนเฉพาะคำที่ยังไม่แม่น
- จิ้มคำตอบหลายคำ, เติมคำในบริบท, ท้าทาย 30 วินาที และจับคู่ความหมาย
- โหมด WebAR Hand Tracking แบบเต็มจอ วาดโครงมือ 21 จุดด้วยเส้นสีม่วง–ฟ้าและไฮไลต์นิ้วที่ใช้คีบตามภาพตัวอย่าง
- ระบบกรองการสั่นแบบปรับตามความเร็วมือ ใช้ระยะคีบเทียบกับขนาดมือ และยืนยันการคีบ/คลายนิ้วต่อเนื่องหลายเฟรมเพื่อลดการหยิบผิด
- ติดตามมือสูงสุดใกล้เคียง 30 FPS ตามกำลังอุปกรณ์ พร้อมลดระยะหน่วงของวงจับ เส้นโครงมือ และคำที่กำลังคีบ
- เล่นซ้ำจากหน้าสรุปด้วยสตรีมกล้องเดิม จึงไม่ขอสิทธิ์กล้องใหม่ทุกรอบ และหยุดกล้องทันทีเมื่อกดออกจากหน้า AR
- วงจับแสดงระดับการคีบแบบเรียลไทม์ ปรับตำแหน่งให้ตรงกับภาพกล้องหน้าและการครอปเต็มจอ
- คำที่อยู่ใกล้วงจับจะเรืองแสงก่อนหยิบ และขยายพื้นที่ปล่อยรอบช่องคำตอบเพื่อให้ควบคุมง่ายบนโทรศัพท์
- ปุ่ม `🤏` เปิด/ปิดเซนเซอร์ และปุ่ม `✋` แสดง/ซ่อนเส้นโครงมือระหว่างเล่น
- การแตะและลากคำยังใช้ได้ตลอดเวลา หากเครื่องช้า แสงน้อย หรือระบบตรวจจับมือโหลดไม่สำเร็จ
- มีแหล่งโหลดระบบตรวจจับมือสำรองอัตโนมัติ หากแหล่งหลักเชื่อมต่อไม่ได้
- มีโหมด AR จำลองสำหรับเครื่องที่ไม่มีกล้องหรือไม่อนุญาตใช้กล้อง ภาพจากกล้องไม่ถูกบันทึกหรือส่งขึ้นระบบ
- คลังคำที่ตอบผิดจำแยกตามด่าน แล้วสร้างรอบทบทวนพร้อมตัดคำที่จำได้ออกให้อัตโนมัติ
- ปุ่ม ⓘ ข้างคำเปิดบัตรคำอธิบาย: ความหมาย ตัวอย่างประโยค ระดับภาษา ชนิดการใช้ และฟังเสียงภาษาไทย
- เลือกความเร็ววิดีโอ 0.75×–2× พร้อมบันทึกค่าที่เลือกไว้ในเครื่อง
- ศูนย์คะแนนนักเรียน บันทึกชื่อ ชั้น/ห้อง เลขที่ บทเรียน โหมด คะแนน และคำที่ยังไม่แม่น
- ดาวน์โหลดคะแนนเป็น CSV หรือส่งไฟล์คะแนนจากโทรศัพท์ให้ครูได้
- ส่งคะแนนจากนักเรียนหลายเครื่องไปรวมใน Google Sheets อัตโนมัติ
- หน้าแดชบอร์ดครูล็อกด้วย PIN พร้อมสถิติภาพรวม ค้นหา กรองห้อง/บทเรียน/โหมด/วันที่ วิเคราะห์คำที่ตอบผิด และดาวน์โหลด CSV
- แถบความก้าวหน้า เหรียญความสำเร็จ โหมดมืด แชร์คะแนน และล้างประวัติการเล่น
- รองรับหน้าจอโทรศัพท์และแท็บเล็ต

## เปิดระบบคะแนนออนไลน์

ก่อนใช้งานจริง ครูต้องตั้งค่า Google Sheet และ Apps Script หนึ่งครั้งตามไฟล์ `google-sheets/SETUP.md` จากนั้นนำลิงก์ `/exec` มาใส่ใน `score-config.js` แล้วอัปโหลดไฟล์ที่แก้แล้วขึ้น GitHub อีกครั้ง ปุ่ม `แดชบอร์ดครู` บนหน้าเกมจะเปิดหน้ารวมคะแนนให้ทันที

## การใช้งาน WebAR บนโทรศัพท์

- เปิดเว็บจากลิงก์ GitHub Pages ที่ขึ้นต้นด้วย `https://`
- ดูวิดีโอให้จบและกรอกข้อมูลนักเรียนก่อน จากนั้นเลือกโหมด `AR ล่าคำ`
- กด `เปิดกล้องและเริ่มคีบคำ` แล้วอนุญาตสิทธิ์กล้องและการเคลื่อนไหวเมื่อโทรศัพท์ถาม
- ใช้กล้องหน้า ยกมือให้อยู่ในเฟรมและเห็นทั้งฝ่ามือ รอจนเส้นโครงมือ 21 จุดปรากฏและสถานะขึ้น `มือพร้อม`
- เลื่อนมือให้วงกลมอยู่ใกล้คำ จากนั้นคีบนิ้วโป้งกับนิ้วชี้เพื่อหยิบ ขยับมือไปเหนือช่องคำตอบ แล้วคลายนิ้วเพื่อปล่อย
- หากหยิบยาก ให้เพิ่มแสง เว้นระยะจากกล้องเล็กน้อย และอย่าให้มือออกนอกเฟรม
- ปุ่ม `🤏` ใช้เปิดหรือปิดเซนเซอร์มือระหว่างเล่น โดยยังสามารถแตะหรือลากคำบนจอได้เสมอ
- ปุ่ม `✋` ใช้แสดงหรือซ่อนเส้นโครงมือ โดยการตรวจจับและการคีบยังทำงานต่อเนื่อง
- ปุ่ม `⇄` ใช้สลับกล้องหน้าและกล้องหลังระหว่างเล่น
- หากไม่ต้องการเปิดกล้อง ให้เลือก `เล่นโดยไม่ใช้กล้อง` เกมและการบันทึกคะแนนยังทำงานครบ
- แนะนำ Chrome/Edge รุ่นล่าสุดบน Android หรือ Safari รุ่นล่าสุดบน iPhone และควรเปิดอินเทอร์เน็ตในการโหลดเซนเซอร์มือครั้งแรก
- การวิเคราะห์ตำแหน่งมือทำในเบราว์เซอร์บนอุปกรณ์ ภาพไม่ถูกบันทึกและไม่ถูกส่งไปยังระบบคะแนน
- หลังเล่นให้กด `×` ออกจากเกม AR เพื่อปิดกล้องและหยุดเซนเซอร์
- ระหว่างกด `เล่น AR อีกครั้ง` ระบบจะใช้กล้องเดิมโดยไม่ถามสิทธิ์ซ้ำ หากออกจากหน้า AR ปิดแท็บ ใช้โหมดไม่ระบุตัวตน หรือตั้งสิทธิ์กล้องแบบครั้งเดียว เบราว์เซอร์อาจถามใหม่ตามนโยบายของเครื่อง
